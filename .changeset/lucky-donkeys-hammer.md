---
"@medusajs/core-flows": patch
"@medusajs/utils": patch
---

fix(core-flows): include `items.variant.metadata` in the create-fulfillment order query, and document the shape of the `items` argument passed to fulfillment providers
