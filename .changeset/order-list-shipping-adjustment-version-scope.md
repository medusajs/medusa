---
"@medusajs/order": patch
---

fix(order): version-scope shipping method adjustments in the select-in (list) path so `GET /admin/orders` returns the same totals as retrieve for fulfilled/shipped orders that have a shipping promotion
