---
"@medusajs/js-sdk": minor
---

Expose store cart addPromotions and removePromotions so promotion codes can be managed through semantic SDK methods instead of manual client.fetch calls, and cover both methods with unit tests.
