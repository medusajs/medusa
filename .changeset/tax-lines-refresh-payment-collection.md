---
"@medusajs/core-flows": minor
---

fix(core-flows): Refresh the cart's payment collection after tax lines are set, and reject cart completion when the payment sessions do not cover the cart total
