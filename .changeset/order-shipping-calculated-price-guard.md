---
"@medusajs/core-flows": patch
---

fix(core-flows): return a clear error instead of a 500 when adding a calculated shipping option without a custom amount to order edits, returns, claims, and exchanges
