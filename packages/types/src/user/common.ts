/**
 * @interface
 *
 * The user details.
 */
export type UserDTO = {
  /**
   * The ID of the user.
   */
  id: string
}

/**
 * @interface
 *
 * The filters to apply on the retrieved user.
 */
export type FilterableUserProps = {}
