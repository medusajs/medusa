import { MedusaError } from "medusa-core-utils"

export default (req, res) => {
  return (err, req, res, next) => {}
}

// app.use(permissions())
// "/admin/permissions"
// endpoint, role
//
// GET "/admin/permissions/product_editor"
