{
  "name": "@medusajs/types",
  "version": "1.11.16",
  "description": "Medusa Types definition",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/medusajs/medusa",
    "directory": "packages/types"
  },
  "engines": {
    "node": ">=16"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "author": "Medusa",
  "license": "MIT",
  "devDependencies": {
    "awilix": "^8.0.0",
    "bignumber.js": "^9.1.2",
    "cross-env": "^5.2.1",
    "ioredis": "^5.2.5",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6",
    "vite": "^5.2.11",
    "winston": "^3.8.2"
  },
  "//": "@medusajs/utils will only be used to import types from it and gets removed at runtime so there’s no remnant of it at runtime",
  "peerDependencies": {
    "@medusajs/utils": "^1.11.9"
  },
  "// 2": "Prevent circular deps",
  "peerDependenciesMeta": {
    "@medusajs/utils": {
      "optional": true
    }
  },
  "scripts": {
    "prepublishOnly": "cross-env NODE_ENV=production tsc --build",
    "build": "rimraf dist && tsc --build",
    "watch": "tsc --build --watch",
    "test": "exit 0"
  }
}
