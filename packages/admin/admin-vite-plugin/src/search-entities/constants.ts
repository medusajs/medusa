/**
 * File names (relative to a source's `src/admin`) that register search
 * entities, in lookup priority order. Only the first match per source is used.
 */
export const SEARCH_ENTITY_FILES = [
  "search-entities.tsx",
  "search-entities.ts",
] as const
