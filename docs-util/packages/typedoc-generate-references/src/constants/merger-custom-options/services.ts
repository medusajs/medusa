import { FormattingOptionsType } from "types"

const servicesOptions: FormattingOptionsType = {
  "^services": {
    frontmatterData: {
      displayed_sidebar: "servicesSidebar",
    },
    maxLevel: 1,
  },
}

export default servicesOptions
