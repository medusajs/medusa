export const metadata = {
  title: `${pageNumber} Example: Add Brands to Medusa`,
}

# {metadata.title}

In the next chapters, you'll follow an example to add brands to Medusa and allow admin users to create a brand through three steps:

1. Add a Brand Module to store and manage brands.
2. Add a workflow to create a brand.
3. Expose an API route to allow admin users to create a brand.
