---
generate_toc: true
---

import ChangelogList from "./content"

export const metadata = {
  title: `Cloud Changelog`,
}

# {metadata.title}

In this guide, you'll find the notable changes to the Cloud dashboard, with the newest change listed first. Each entry lists the changes that went live on its date.

Changes to other parts of Cloud have their own changelogs:

- [Cloud CLI Changelog](../cli/changelog/page.mdx) for changes to the `mcloud` CLI.
- [Webhooks Changelog](../webhooks/changelog/page.mdx) for changes to the webhook events that Cloud delivers.

<ChangelogList />
