---
displayed_sidebar: regionReference
---

import TypeList from "@site/src/components/TypeList"

# Primary

 **Primary**: T extends `object` ? [ReadonlyPrimary](region.ReadonlyPrimary.mdx)&#60;PK&#62; : T extends `object` ? [ReadonlyPrimary](region.ReadonlyPrimary.mdx)&#60;PK&#62; \| `string` : T extends `object` ? [ReadonlyPrimary](region.ReadonlyPrimary.mdx)&#60;PK&#62; : T extends `object` ? [ReadonlyPrimary](region.ReadonlyPrimary.mdx)&#60;PK&#62; : `never`
