import TypeList from "@site/src/components/TypeList"

# ComputeActionOptions

Options to configure how actions are computed.

## Properties

<TypeList types={[{"name":"prevent_auto_promotions","type":"`boolean`","description":"Whether to apply promotions having their `is_automatic` field enabled\nautomatically. If not provided, the automatic promotions are applied.","optional":true,"defaultValue":"","expandable":false,"children":[]}]}  sectionTitle="ComputeActionOptions"/>
