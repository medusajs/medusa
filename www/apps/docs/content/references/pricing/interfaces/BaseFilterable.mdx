---
displayed_sidebar: pricingReference
---

# BaseFilterable

An object used to allow specifying flexible queries with and/or conditions.

## Type parameters

| Name |
| :------ |
| `T` | `object` |

## Hierarchy

- **`BaseFilterable`**

  ↳ [`FilterablePriceSetProps`](FilterablePriceSetProps.mdx)

  ↳ [`FilterableMoneyAmountProps`](FilterableMoneyAmountProps.mdx)

  ↳ [`FilterableCurrencyProps`](FilterableCurrencyProps.mdx)

  ↳ [`FilterableRuleTypeProps`](FilterableRuleTypeProps.mdx)

  ↳ [`FilterablePriceSetMoneyAmountRulesProps`](FilterablePriceSetMoneyAmountRulesProps.mdx)

  ↳ [`FilterablePriceRuleProps`](FilterablePriceRuleProps.mdx)

## Properties

### $and

 `Optional` **$and**: (`T` \| [`BaseFilterable`](BaseFilterable.mdx)<`T`\>)[]

An array of filters to apply on the entity, where each item in the array is joined with an "and" condition.

___

### $or

 `Optional` **$or**: (`T` \| [`BaseFilterable`](BaseFilterable.mdx)<`T`\>)[]

An array of filters to apply on the entity, where each item in the array is joined with an "or" condition.
