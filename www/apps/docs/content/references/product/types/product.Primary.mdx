---
displayed_sidebar: productReference
---

import ParameterTypes from "@site/src/components/ParameterTypes"

# Primary

 **Primary**: `T` extends `object` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; : `T` extends `object` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; \| `string` : `T` extends `object` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; : `T` extends `object` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; : `never`
