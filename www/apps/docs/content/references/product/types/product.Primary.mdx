---
displayed_sidebar: productReference
---

import ParameterTypes from "@site/src/components/ParameterTypes"

# Primary

 **Primary**: `T` extends ``{ [PrimaryKeyType]?: infer PK  }`` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; : `T` extends ``{ _id?: infer PK  }`` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; \| `string` : `T` extends ``{ uuid?: infer PK  }`` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; : `T` extends ``{ id?: infer PK  }`` ? [ReadonlyPrimary](product.ReadonlyPrimary.mdx)&#60;PK&#62; : `never`
