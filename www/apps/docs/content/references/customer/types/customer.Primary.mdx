---
displayed_sidebar: customerReference
---

import TypeList from "@site/src/components/TypeList"

# Primary

 **Primary**: T extends `object` ? [ReadonlyPrimary](customer.ReadonlyPrimary.mdx)&#60;PK&#62; : T extends `object` ? [ReadonlyPrimary](customer.ReadonlyPrimary.mdx)&#60;PK&#62; \| `string` : T extends `object` ? [ReadonlyPrimary](customer.ReadonlyPrimary.mdx)&#60;PK&#62; : T extends `object` ? [ReadonlyPrimary](customer.ReadonlyPrimary.mdx)&#60;PK&#62; : `never`
