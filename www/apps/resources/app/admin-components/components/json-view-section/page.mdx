---
sidebar_label: "JSON View Section"
---

import { TypeList } from "docs-ui"

export const metadata = {
  title: `JsonViewSection - Admin Components`,
}

# {metadata.title}

<Note>

This component is available since Medusa [v2.21.0](https://github.com/medusajs/medusa/releases/tag/v2.21.0).

</Note>

The `JsonViewSection` component shows a section with the number of keys in an object, and a button that opens a drawer with the object's formatted JSON. The Medusa Admin shows this section at the bottom of its detail pages.

```tsx
import { JsonViewSection } from "@medusajs/dashboard/components"
```

---

## Example

Use `JsonViewSection` in a [widget](!docs!/learn/fundamentals/admin/widgets) or [UI route](!docs!/learn/fundamentals/admin/ui-routes) to let the user inspect a record's raw data:

```tsx
import {
  JsonViewSection,
} from "@medusajs/dashboard/components"

const BrandJsonSection = ({ brand }) => {
  return <JsonViewSection data={brand} />
}
```

In the drawer, Medusa collapses nested objects beyond the first level, and the user can click the copy icon next to a value to copy it to the clipboard.

---

## Props

The component accepts the `JsonViewSectionProps` type:

<TypeList
  types={[
    {
      name: "data",
      type: "`object`",
      optional: false,
      description:
        "The object to show in JSON format. Its keys and values depend on the record you pass.",
    },
    {
      name: "title",
      type: "`string`",
      optional: true,
      description:
        "An optional title. The component accepts it, but it doesn't render it. Medusa always shows its own JSON heading instead.",
    },
  ]}
/>
