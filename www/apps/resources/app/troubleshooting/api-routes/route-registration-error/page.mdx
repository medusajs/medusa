import RouteRegistrationError from "../../_sections/api-routes/route-registration-error.mdx"

export const metadata = {
  title: `API Route Registration Error`,
}

# {metadata.title}

<RouteRegistrationError />
