import { TypeList } from "docs-ui"

# isDmlEntity

A static method to check if an entity is an instance of DmlEntity.
It allows us to identify a specific object as being an instance of
DmlEntity.

## Parameters

<TypeList types={[{"name":"entity","type":"`unknown`","description":"","optional":false,"defaultValue":"","expandable":false,"children":[]}]} expandUrl="https://docs.medusajs.com/v2/advanced-development/data-models/manage-relationships#retrieve-records-of-relation" sectionTitle="isDmlEntity"/>
