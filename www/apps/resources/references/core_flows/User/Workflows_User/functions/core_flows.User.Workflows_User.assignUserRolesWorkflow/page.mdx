---
slug: /references/medusa-workflows/assignUserRolesWorkflow
sidebar_label: assignUserRolesWorkflow
sidebar_description: ""
tags:
  - query
  - link
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# assignUserRolesWorkflow - Medusa Core Workflows Reference

:::note

To use this resource, make sure to [enable its feature flag: rbac](https://docs.medusajs.com/development/feature-flags/toggle)

:::

This documentation provides a reference to the `assignUserRolesWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

This workflow assigns roles to users.
Supports two modes:
- Assign multiple roles to a single user: &#123; user\_id, role\_ids &#125;
- Assign multiple users to a single role: &#123; user\_ids, role\_id &#125;
It validates that the actor has all the policies from the roles being assigned.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/735e0aa51ef64274257057a59046776af54203f4/packages/core/core-flows/src/user/workflows/assign-user-roles.ts#L36" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { assignUserRolesWorkflow } from "@medusajs/medusa/core-flows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  const { result } = await assignUserRolesWorkflow(req.scope)
    .run({
      input: {}
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { assignUserRolesWorkflow } from "@medusajs/medusa/core-flows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  const { result } = await assignUserRolesWorkflow(container)
    .run({
      input: {}
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { assignUserRolesWorkflow } from "@medusajs/medusa/core-flows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  const { result } = await assignUserRolesWorkflow(container)
    .run({
      input: {}
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { assignUserRolesWorkflow } from "@medusajs/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const result = assignUserRolesWorkflow
      .runAsStep({
        input: {}
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"assignUserRolesWorkflow","steps":[{"type":"step","name":"validateUserRolePermissionsStep","description":"Validates that the actor has all the policies from the roles being assigned.\nA user can only assign roles whose policies they themselves have.","link":"../../../Steps_User/functions/core_flows.User.Steps_User.validateUserRolePermissionsStep/page.mdx","depth":1},{"type":"step","name":"createRemoteLinkStep","description":"This step creates links between two records of linked data models.\n\nLearn more in the [Link documentation.](https://docs.medusajs.com/learn/fundamentals/module-links/link#create-link).","link":"../../../../Common/Steps_Common/functions/core_flows.Common.Steps_Common.createRemoteLinkStep/page.mdx","depth":2}]}} />

## Input

<TypeList types={[{"name":"AssignUserRolesWorkflowInput","type":"[AssignUserRolesWorkflowInput](../../../../core_core_flows_src/types/core_flows.core_core_flows_src.AssignUserRolesWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"AssignUserRolesWorkflowInput","type":"`object`","description":"","optional":false,"defaultValue":"","expandable":false,"featureFlag":"rbac","children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="assignUserRolesWorkflow"/>
