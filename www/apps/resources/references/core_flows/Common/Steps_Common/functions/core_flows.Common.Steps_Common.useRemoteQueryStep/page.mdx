---
slug: /references/medusa-workflows/steps/useRemoteQueryStep
sidebar_label: useRemoteQueryStep
tags:
  - remote query
  - step
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# useRemoteQueryStep - Medusa Core Workflows Reference

This documentation provides a reference to the `useRemoteQueryStep`. It belongs to the `@medusajs/medusa/core-flows` package.

This step fetches data across modules using the remote query.

Learn more in the [Remote Query documentation](https://docs.medusajs.com/learn/fundamentals/module-links/query).

:::note

This step is deprecated. Use [useQueryGraphStep](../core_flows.Common.Steps_Common.useQueryGraphStep/page.mdx) instead.

:::

## Example

To retrieve a list of records of a data model:

```ts
const products = useRemoteQueryStep({
  entry_point: "product",
  fields: [
    "*",
    "variants.*"
  ]
})
```

To retrieve a single item instead of a an array:

```ts
const product = useRemoteQueryStep({
  entry_point: "product",
  fields: [
    "*",
    "variants.*"
  ],
  variables: {
    filters: {
      id: "123"
    }
  },
  list: false
})
```

To throw an error if a record isn't found matching the specified ID:

```ts
const product = useRemoteQueryStep({
  entry_point: "product",
  fields: [
    "*",
    "variants.*"
  ],
  variables: {
    filters: {
      id: "123"
    }
  },
  list: false,
  throw_if_key_not_found: true
})
```

## Input

<TypeList types={[{"name":"EntryStepInput \\| ServiceStepInput","type":"[EntryStepInput](../../../../core_core_flows_src/interfaces/core_flows.core_core_flows_src.EntryStepInput/page.mdx) \\| [ServiceStepInput](../../../../core_core_flows_src/interfaces/core_flows.core_core_flows_src.ServiceStepInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"EntryStepInput \\| ServiceStepInput","type":"[EntryStepInput](../../../../core_core_flows_src/interfaces/core_flows.core_core_flows_src.EntryStepInput/page.mdx) \\| [ServiceStepInput](../../../../core_core_flows_src/interfaces/core_flows.core_core_flows_src.ServiceStepInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="useRemoteQueryStep"/>
