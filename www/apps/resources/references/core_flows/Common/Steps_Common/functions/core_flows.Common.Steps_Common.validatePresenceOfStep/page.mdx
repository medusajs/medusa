---
slug: /references/medusa-workflows/steps/validatePresenceOfStep
sidebar_label: validatePresenceOfStep
tags:
  - step
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# validatePresenceOfStep - Medusa Core Workflows Reference

This documentation provides a reference to the `validatePresenceOfStep`. It belongs to the `@medusajs/medusa/core-flows` package.

This step validates the presence of attributes on an object

```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { validatePresenceOfStep } from "@medusajs/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const data = validatePresenceOfStep({})
  }
)
```
