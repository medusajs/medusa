---
slug: /references/medusa-workflows/computeCreditLineActionsStep
sidebar_label: computeCreditLineActionsStep
sidebar_description: ""
tags:
  - step
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# computeCreditLineActionsStep - Medusa Core Workflows Reference

This documentation provides a reference to the `computeCreditLineActionsStep`. It belongs to the `@medusajs/medusa/core-flows` package.

This step computes the credit lines to create and delete when applying store
credits to a cart. It collects the existing store-credit lines to delete and
builds a new credit line for the specified amount, or for the full store credit
balance if no amount is specified. It throws an error if the requested amount is
greater than the store credit account balance.

## Example

```ts
const data = computeCreditLineActionsStep({
  storeCreditAccount: {
    id: "sca_123",
    balance: 100,
    // other store credit account properties...
  },
  cart: { ...cart, id: "cart_123", total: 50 },
  input: { amount: 50 },
})
```

## Input

<TypeList types={[{"name":"ComputeCreditLineActionsStepInput","type":"[ComputeCreditLineActionsStepInput](../../../../plugins_loyalty_src_workflows/interfaces/core_flows.plugins_loyalty_src_workflows.ComputeCreditLineActionsStepInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"ComputeCreditLineActionsStepInput","type":"`object`","description":"Input to compute the credit line actions to apply store credits to a cart.","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="computeCreditLineActionsStep"/>

## Output

<TypeList types={[{"name":"WorkflowDataProperties","type":"[WorkflowDataProperties](../../../../../workflows/types/workflows.WorkflowDataProperties/page.mdx)&#60;object&#62;","optional":false,"defaultValue":"","description":"","expandable":false,"children":[]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="computeCreditLineActionsStep"/>
