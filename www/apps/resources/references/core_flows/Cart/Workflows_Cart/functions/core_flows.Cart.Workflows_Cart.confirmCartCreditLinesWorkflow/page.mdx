---
slug: /references/medusa-workflows/confirmCartCreditLinesWorkflow
sidebar_label: confirmCartCreditLinesWorkflow
sidebar_description: Confirm and debit the credit lines on a cart.
tags:
  - workflow
  - store credit
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# confirmCartCreditLinesWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `confirmCartCreditLinesWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

This workflow confirms the credit lines of a cart by debiting the associated
store credit and gift card accounts for the credit line amounts. It is typically
called before payment authorization to lock in the store credit usage.

You can use this workflow within your own customizations or custom workflows,
allowing you to wrap custom logic around confirming cart credit lines.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/699a9a85c795e13f9e787056e0fb65cfea0115dc/packages/plugins/loyalty/src/workflows/carts/workflows/confirm-cart-credit-lines.ts#L98" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { confirmCartCreditLinesWorkflow } from "@medusajs/loyalty-plugin/workflows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  const { result } = await confirmCartCreditLinesWorkflow(req.scope)
    .run({
      input: {
        cart_id: "cart_123",
      },
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { confirmCartCreditLinesWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  const { result } = await confirmCartCreditLinesWorkflow(container)
    .run({
      input: {
        cart_id: "cart_123",
      },
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { confirmCartCreditLinesWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  const { result } = await confirmCartCreditLinesWorkflow(container)
    .run({
      input: {
        cart_id: "cart_123",
      },
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { confirmCartCreditLinesWorkflow } from "@medusajs/loyalty-plugin/workflows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const result = confirmCartCreditLinesWorkflow
      .runAsStep({
        input: {
          cart_id: "cart_123",
        },
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"confirmCartCreditLinesWorkflow","steps":[{"type":"workflow","name":"debitAccountsWorkflow","description":"Debit one or more store credit accounts.","link":"../../../../Store_Credit/Workflows_Store_Credit/functions/core_flows.Store_Credit.Workflows_Store_Credit.debitAccountsWorkflow/page.mdx","depth":1}]}} />

## Input

<TypeList types={[{"name":"ConfirmCartCreditLinesWorkflowInput","type":"[ConfirmCartCreditLinesWorkflowInput](../../../../plugins_loyalty_src_workflows/interfaces/core_flows.plugins_loyalty_src_workflows.ConfirmCartCreditLinesWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"ConfirmCartCreditLinesWorkflowInput","type":"`object`","description":"Input to confirm and debit the credit lines on a cart.","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="confirmCartCreditLinesWorkflow"/>

## Output

<TypeList types={[{"name":"[]","type":"[]","optional":false,"defaultValue":"","description":"","expandable":false,"children":[]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="confirmCartCreditLinesWorkflow"/>
