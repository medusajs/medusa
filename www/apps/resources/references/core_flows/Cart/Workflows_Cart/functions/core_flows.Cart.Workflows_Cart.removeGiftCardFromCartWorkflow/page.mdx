---
slug: /references/medusa-workflows/removeGiftCardFromCartWorkflow
sidebar_label: removeGiftCardFromCartWorkflow
sidebar_description: Remove a gift card from a cart.
tags:
  - workflow
  - link
  - locking
  - query
  - cart
  - fulfillment
  - tax
  - promotion
  - remote query
  - payment
  - logger
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# removeGiftCardFromCartWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `removeGiftCardFromCartWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

This workflow removes a gift card from a cart by deleting its associated credit
lines, dismissing the links between the cart and the gift card, and refreshing
the cart items.

You can use this workflow within your own customizations or custom workflows,
allowing you to wrap custom logic around removing gift cards from carts.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/699a9a85c795e13f9e787056e0fb65cfea0115dc/packages/plugins/loyalty/src/workflows/carts/workflows/remove-gift-cart-from-cart.ts#L156" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { removeGiftCardFromCartWorkflow } from "@medusajs/loyalty-plugin/workflows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  await removeGiftCardFromCartWorkflow(req.scope)
    .run({
      input: {
        code: "GC-XXXX-XXXX",
        cart_id: "cart_123",
      },
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { removeGiftCardFromCartWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  await removeGiftCardFromCartWorkflow(container)
    .run({
      input: {
        code: "GC-XXXX-XXXX",
        cart_id: "cart_123",
      },
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { removeGiftCardFromCartWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  await removeGiftCardFromCartWorkflow(container)
    .run({
      input: {
        code: "GC-XXXX-XXXX",
        cart_id: "cart_123",
      },
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { removeGiftCardFromCartWorkflow } from "@medusajs/loyalty-plugin/workflows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    removeGiftCardFromCartWorkflow
      .runAsStep({
        input: {
          code: "GC-XXXX-XXXX",
          cart_id: "cart_123",
        },
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"removeGiftCardFromCartWorkflow","steps":[{"type":"step","name":"validateGiftCardStep","description":"This step validates that a gift card code exists in the cart and that the gift card\nitself exists. It throws an error if the gift card code is not in the cart or if\nthe gift card record is not found.","link":"../core_flows.Cart.Workflows_Cart.validateGiftCardStep/page.mdx","depth":1},{"type":"step","name":"validateGiftCardInCartStep","description":"This step validates that a gift card is present in a cart. It throws an error\nif the gift card is not found in the cart's gift cards.","link":"../core_flows.Cart.Workflows_Cart.validateGiftCardInCartStep/page.mdx","depth":2},{"type":"workflow","name":"deleteCartCreditLinesWorkflow","description":"This workflow deletes one or more credit lines from a cart.","link":"../core_flows.Cart.Workflows_Cart.deleteCartCreditLinesWorkflow/page.mdx","depth":3},{"type":"workflow","name":"dismissLinksWorkflow","description":"Dismiss links between two records of linked data models.","link":"../../../../Common/Workflows_Common/functions/core_flows.Common.Workflows_Common.dismissLinksWorkflow/page.mdx","depth":4},{"type":"workflow","name":"refreshCartItemsWorkflow","description":"Refresh a cart's details after an update.","link":"../core_flows.Cart.Workflows_Cart.refreshCartItemsWorkflow/page.mdx","depth":5}]}} />

## Input

<TypeList types={[{"name":"RemoveGiftCardFromCartWorkflowInput","type":"[RemoveGiftCardFromCartWorkflowInput](../../../../plugins_loyalty_src_workflows/interfaces/core_flows.plugins_loyalty_src_workflows.RemoveGiftCardFromCartWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"RemoveGiftCardFromCartWorkflowInput","type":"`object`","description":"Input to remove a gift card from a cart.","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="removeGiftCardFromCartWorkflow"/>
