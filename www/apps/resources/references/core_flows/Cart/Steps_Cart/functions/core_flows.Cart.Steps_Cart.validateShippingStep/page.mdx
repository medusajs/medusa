---
slug: /references/medusa-workflows/steps/validateShippingStep
sidebar_label: validateShippingStep
tags:
  - step
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# validateShippingStep - Medusa Core Workflows Reference

This documentation provides a reference to the `validateShippingStep`. It belongs to the `@medusajs/medusa/core-flows` package.

This step validates shipping data when cart is completed.

It ensures that a shipping method is selected if there is an item in the cart that requires shipping.
It also ensures that product's shipping profile mathes the selected shipping options. If the
conditions are not met, an error is thrown.

:::note

You can retrieve cart or shipping option's details using [Query](https://docs.medusajs.com/learn/fundamentals/module-links/query),
or [useQueryGraphStep](https://docs.medusajs.com/resources/references/medusa-workflows/steps/useQueryGraphStep).

:::

## Example

```ts
validateShippingStep({
  cart: {
    id: "cart_123",
    items: [
      {
        id: "item_123",
        variant: {
          id: "variant_123",
          // other item details...
        },
      }
    ],
    // other cart details...
  },
  shippingOptions: [
    {
      id: "option_123",
      shipping_profile_id: "sp_123",
      // other option details...
    }
  ]
})
```

## Input

<TypeList types={[{"name":"ValidateShippingInput","type":"[ValidateShippingInput](../../../../core_core_flows_src/types/core_flows.core_core_flows_src.ValidateShippingInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"ValidateShippingInput","type":"`object`","description":"The data to validate shipping data when cart is completed.","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="validateShippingStep"/>
