---
slug: /references/medusa-workflows/updateGiftCardsWorkflow
sidebar_label: updateGiftCardsWorkflow
sidebar_description: Update gift cards.
tags:
  - loyalty
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# updateGiftCardsWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `updateGiftCardsWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

This workflow updates one or more gift cards.

You can use this workflow within your own customizations or custom workflows,
allowing you to wrap custom logic around gift card updates.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/1cbd983356e34ccf3398f26599c6a0f77568cf9e/packages/plugins/loyalty/src/workflows/gift-cards/workflows/update-gift-cards.ts#L32" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { updateGiftCardsWorkflow } from "@medusajs/loyalty-plugin/workflows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  const { result } = await updateGiftCardsWorkflow(req.scope)
    .run({
      input: [{
        id: "gc_123",
        status: GiftCardStatus.REDEEMED,
        // other updatable gift card properties...
      }, ],
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { updateGiftCardsWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  const { result } = await updateGiftCardsWorkflow(container)
    .run({
      input: [{
        id: "gc_123",
        status: GiftCardStatus.REDEEMED,
        // other updatable gift card properties...
      }, ],
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { updateGiftCardsWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  const { result } = await updateGiftCardsWorkflow(container)
    .run({
      input: [{
        id: "gc_123",
        status: GiftCardStatus.REDEEMED,
        // other updatable gift card properties...
      }, ],
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { updateGiftCardsWorkflow } from "@medusajs/loyalty-plugin/workflows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const result = updateGiftCardsWorkflow
      .runAsStep({
        input: [{
          id: "gc_123",
          status: GiftCardStatus.REDEEMED,
          // other updatable gift card properties...
        }, ],
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"updateGiftCardsWorkflow","steps":[{"type":"step","name":"updateGiftCardsStep","description":"This step updates gift cards. It stores the previous state of the gift cards\nbefore the update to enable rollback.","link":"../../../Steps_Gift_Card/functions/core_flows.Gift_Card.Steps_Gift_Card.updateGiftCardsStep/page.mdx","depth":1}]}} />

## Input

<TypeList types={[{"name":"UpdateGiftCardsWorkflowInput","type":"[UpdateGiftCardsWorkflowInput](../../../../plugins_loyalty_src_workflows/types/core_flows.plugins_loyalty_src_workflows.UpdateGiftCardsWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"UpdateGiftCardsWorkflowInput","type":"`ModuleUpdateGiftCard`[]","description":"Data for updating one or more gift cards.","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="updateGiftCardsWorkflow"/>

## Output

<TypeList types={[{"name":"ModuleGiftCard[]","type":"`ModuleGiftCard`[]","optional":false,"defaultValue":"","description":"","expandable":false,"children":[]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="updateGiftCardsWorkflow"/>
