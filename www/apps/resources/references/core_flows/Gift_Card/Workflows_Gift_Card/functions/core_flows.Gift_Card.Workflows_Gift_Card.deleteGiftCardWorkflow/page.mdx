---
slug: /references/medusa-workflows/deleteGiftCardWorkflow
sidebar_label: deleteGiftCardWorkflow
sidebar_description: Delete a gift card.
tags:
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# deleteGiftCardWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `deleteGiftCardWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

This workflow deletes a gift card by its ID.

You can use this workflow within your own customizations or custom workflows,
allowing you to wrap custom logic around gift card deletion.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/699a9a85c795e13f9e787056e0fb65cfea0115dc/packages/plugins/loyalty/src/workflows/gift-cards/workflows/delete-gift-card.ts#L33" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { deleteGiftCardWorkflow } from "@medusajs/loyalty-plugin/workflows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  await deleteGiftCardWorkflow(req.scope)
    .run({
      input: {
        id: "gc_123",
      },
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { deleteGiftCardWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  await deleteGiftCardWorkflow(container)
    .run({
      input: {
        id: "gc_123",
      },
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { deleteGiftCardWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  await deleteGiftCardWorkflow(container)
    .run({
      input: {
        id: "gc_123",
      },
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { deleteGiftCardWorkflow } from "@medusajs/loyalty-plugin/workflows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    deleteGiftCardWorkflow
      .runAsStep({
        input: {
          id: "gc_123",
        },
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"deleteGiftCardWorkflow","steps":[{"type":"step","name":"deleteGiftCardsStep","description":"This step deletes gift cards by their IDs. The step supports rollback by\nrestoring the deleted gift cards.","link":"../../../Steps_Gift_Card/functions/core_flows.Gift_Card.Steps_Gift_Card.deleteGiftCardsStep/page.mdx","depth":1}]}} />

## Input

<TypeList types={[{"name":"DeleteGiftCardWorkflowInput","type":"[DeleteGiftCardWorkflowInput](../../../../plugins_loyalty_src_workflows/interfaces/core_flows.plugins_loyalty_src_workflows.DeleteGiftCardWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"DeleteGiftCardWorkflowInput","type":"`object`","description":"The data to delete a gift card.","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="deleteGiftCardWorkflow"/>
