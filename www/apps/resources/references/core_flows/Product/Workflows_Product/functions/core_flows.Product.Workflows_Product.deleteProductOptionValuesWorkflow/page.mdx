---
slug: /references/medusa-workflows/deleteProductOptionValuesWorkflow
sidebar_label: deleteProductOptionValuesWorkflow
sidebar_description: Delete one or more product option values.
tags:
  - product
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# deleteProductOptionValuesWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `deleteProductOptionValuesWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

This workflow deletes one or more product option values. It's used by the
Delete Product Option Value Admin API Route.

You can use this workflow within your customizations or your own custom workflows,
allowing you to wrap custom logic around product-option-value deletion.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/9aadd07b63d005d57b912b202144ef3f96bf2c90/packages/core/core-flows/src/product/workflows/delete-product-option-values.ts#L39" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { deleteProductOptionValuesWorkflow } from "@medusajs/medusa/core-flows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  const { result } = await deleteProductOptionValuesWorkflow(req.scope)
    .run({
      input: {
        ids: ["optval_123"]
      }
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { deleteProductOptionValuesWorkflow } from "@medusajs/medusa/core-flows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  const { result } = await deleteProductOptionValuesWorkflow(container)
    .run({
      input: {
        ids: ["optval_123"]
      }
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { deleteProductOptionValuesWorkflow } from "@medusajs/medusa/core-flows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  const { result } = await deleteProductOptionValuesWorkflow(container)
    .run({
      input: {
        ids: ["optval_123"]
      }
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { deleteProductOptionValuesWorkflow } from "@medusajs/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const result = deleteProductOptionValuesWorkflow
      .runAsStep({
        input: {
          ids: ["optval_123"]
        }
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"deleteProductOptionValuesWorkflow","steps":[{"type":"step","name":"deleteProductOptionValuesStep","description":"This step deletes one or more product option values.","link":"../../../Steps_Product/functions/core_flows.Product.Steps_Product.deleteProductOptionValuesStep/page.mdx","depth":1}]}} />

## Input

<TypeList types={[{"name":"DeleteProductOptionValuesWorkflowInput","type":"[DeleteProductOptionValuesWorkflowInput](../../../../core_core_flows_src/types/core_flows.core_core_flows_src.DeleteProductOptionValuesWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"DeleteProductOptionValuesWorkflowInput","type":"`object`","description":"The data to delete one or more product option values.","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="deleteProductOptionValuesWorkflow"/>
