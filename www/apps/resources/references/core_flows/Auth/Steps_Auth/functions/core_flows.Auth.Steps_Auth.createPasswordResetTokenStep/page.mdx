---
slug: /references/medusa-workflows/steps/createPasswordResetTokenStep
sidebar_label: createPasswordResetTokenStep
tags:
  - auth
  - step
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# createPasswordResetTokenStep - Medusa Core Workflows Reference

This documentation provides a reference to the `createPasswordResetTokenStep`. It belongs to the `@medusajs/medusa/core-flows` package.

Issues a single-use password reset token via the auth module. The token's
`jti` is meant to be embedded in the JWT delivered to the user; the auth
module stores its hash in a dedicated table so the consume side can verify
and atomically delete it on first use.

Issuing a new token automatically invalidates any prior password-reset
tokens for the same provider identity. Compensation consumes the token
(best-effort) if the workflow rolls back.

:::note

This is available starting from [Medusa v2.16.0](https://github.com/medusajs/medusa/releases/tag/v2.16.0).

:::

```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { createPasswordResetTokenStep } from "@medusajs/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const data = createPasswordResetTokenStep({})
  }
)
```
  

## Input

<TypeList types={[{"name":"CreatePasswordResetTokenStepInput","type":"[CreatePasswordResetTokenStepInput](../../../../core_core_flows_src/types/core_flows.core_core_flows_src.CreatePasswordResetTokenStepInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"CreatePasswordResetTokenStepInput","type":"`object`","description":"Input for the create password reset token step.","optional":false,"defaultValue":"","expandable":false,"children":[],"since":"2.16.0"}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="createPasswordResetTokenStep"/>

## Output

<TypeList types={[{"name":"CreatePasswordResetTokenStepOutput","type":"[CreatePasswordResetTokenStepOutput](../../../../core_core_flows_src/types/core_flows.core_core_flows_src.CreatePasswordResetTokenStepOutput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"CreatePasswordResetTokenStepOutput","type":"`object`","description":"Output from the create password reset token step.","optional":false,"defaultValue":"","expandable":false,"children":[],"since":"2.16.0"}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="createPasswordResetTokenStep"/>
