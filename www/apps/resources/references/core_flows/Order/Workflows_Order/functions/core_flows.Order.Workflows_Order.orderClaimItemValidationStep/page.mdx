---
slug: /references/medusa-workflows/orderClaimItemValidationStep
sidebar_label: orderClaimItemValidationStep
sidebar_description: ""
tags:
  - step
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# orderClaimItemValidationStep - Medusa Core Workflows Reference

This documentation provides a reference to the `orderClaimItemValidationStep`. It belongs to the `@medusajs/medusa/core-flows` package.

This step validates that claim items can be added to a claim. If the
order or claim is canceled, or the order change is not active, the step will throw an error.

:::note

You can retrieve an order, order claim, and order change details using [Query](https://docs.medusajs.com/learn/fundamentals/module-links/query),
or [useQueryGraphStep](https://docs.medusajs.com/resources/references/medusa-workflows/steps/useQueryGraphStep).

:::

## Example

```ts
const data = orderClaimItemValidationStep({
  order: {
    id: "order_123",
    // other order details...
  },
  orderChange: {
    id: "orch_123",
    // other order change details...
  },
  orderClaim: {
    id: "claim_123",
    // other order claim details...
  }
})
```
