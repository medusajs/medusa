---
slug: /references/medusa-workflows/clearLayoutConfigurationWorkflow
sidebar_label: clearLayoutConfigurationWorkflow
sidebar_description: ""
tags:
  - settings
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# clearLayoutConfigurationWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `clearLayoutConfigurationWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/b67dcab7477cea6b362dcf5e946e6e642d508409/packages/core/core-flows/src/settings/workflows/clear-layout-configuration.ts#L15" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { clearLayoutConfigurationWorkflow } from "@medusajs/medusa/core-flows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  const { result } = await clearLayoutConfigurationWorkflow(req.scope)
    .run({
      input: {}
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { clearLayoutConfigurationWorkflow } from "@medusajs/medusa/core-flows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  const { result } = await clearLayoutConfigurationWorkflow(container)
    .run({
      input: {}
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { clearLayoutConfigurationWorkflow } from "@medusajs/medusa/core-flows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  const { result } = await clearLayoutConfigurationWorkflow(container)
    .run({
      input: {}
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { clearLayoutConfigurationWorkflow } from "@medusajs/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const result = clearLayoutConfigurationWorkflow
      .runAsStep({
        input: {}
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"clearLayoutConfigurationWorkflow","steps":[{"type":"step","name":"clearLayoutConfigurationStep","description":"","link":"../../../Steps_Setting/functions/core_flows.Setting.Steps_Setting.clearLayoutConfigurationStep/page.mdx","depth":1},{"type":"step","name":"setActiveLayoutScopeStep","description":"","link":"../../../Steps_Setting/functions/core_flows.Setting.Steps_Setting.setActiveLayoutScopeStep/page.mdx","depth":2}]}} />

## Input

<TypeList types={[{"name":"ClearLayoutConfigurationWorkflowInput","type":"[ClearLayoutConfigurationWorkflowInput](../../../../core_core_flows_src/types/core_flows.core_core_flows_src.ClearLayoutConfigurationWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"ClearLayoutConfigurationWorkflowInput","type":"`object`","description":"","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="clearLayoutConfigurationWorkflow"/>
