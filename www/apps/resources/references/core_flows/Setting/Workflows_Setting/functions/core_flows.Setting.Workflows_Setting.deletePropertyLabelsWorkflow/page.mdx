---
slug: /references/medusa-workflows/deletePropertyLabelsWorkflow
sidebar_label: deletePropertyLabelsWorkflow
sidebar_description: ""
tags:
  - settings
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# deletePropertyLabelsWorkflow - Medusa Core Workflows Reference

:::note

To use this resource, make sure to [enable its feature flag: view_configurations](https://docs.medusajs.com/development/feature-flags/toggle)

:::

This documentation provides a reference to the `deletePropertyLabelsWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

:::note

This is available starting from [Medusa v2.13.2](https://github.com/medusajs/medusa/releases/tag/v2.13.2).

:::

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/699a9a85c795e13f9e787056e0fb65cfea0115dc/packages/core/core-flows/src/settings/workflows/delete-property-labels.ts#L18" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { deletePropertyLabelsWorkflow } from "@medusajs/medusa/core-flows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  const { result } = await deletePropertyLabelsWorkflow(req.scope)
    .run({
      input: {}
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { deletePropertyLabelsWorkflow } from "@medusajs/medusa/core-flows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  const { result } = await deletePropertyLabelsWorkflow(container)
    .run({
      input: {}
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { deletePropertyLabelsWorkflow } from "@medusajs/medusa/core-flows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  const { result } = await deletePropertyLabelsWorkflow(container)
    .run({
      input: {}
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { deletePropertyLabelsWorkflow } from "@medusajs/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const result = deletePropertyLabelsWorkflow
      .runAsStep({
        input: {}
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"deletePropertyLabelsWorkflow","steps":[{"type":"step","name":"deletePropertyLabelsStep","description":"Workflow step to delete property labels.","link":"../../../Steps_Setting/functions/core_flows.Setting.Steps_Setting.deletePropertyLabelsStep/page.mdx","depth":1}]}} />

## Input

<TypeList types={[{"name":"DeletePropertyLabelsWorkflowInput","type":"[DeletePropertyLabelsWorkflowInput](../../../../core_core_flows_src/interfaces/core_flows.core_core_flows_src.DeletePropertyLabelsWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"DeletePropertyLabelsWorkflowInput","type":"`object`","description":"","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="deletePropertyLabelsWorkflow"/>
