---
slug: /references/medusa-workflows/creditStoreCreditAccountWorkflow
sidebar_label: creditStoreCreditAccountWorkflow
sidebar_description: Credit a store credit account.
tags:
  - query
  - store credit
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# creditStoreCreditAccountWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `creditStoreCreditAccountWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

This workflow credits a specific store credit account with an amount. It validates
that the amount is greater than zero and that the account exists before crediting.

You can use this workflow within your own customizations or custom workflows,
allowing you to wrap custom logic around crediting a store credit account.

<SourceCodeLink link="https://github.com/medusajs/medusa/blob/699a9a85c795e13f9e787056e0fb65cfea0115dc/packages/plugins/loyalty/src/workflows/store-credit/workflows/credit-store-credit-account.ts#L48" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { creditStoreCreditAccountWorkflow } from "@medusajs/loyalty-plugin/workflows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  await creditStoreCreditAccountWorkflow(req.scope)
    .run({
      input: {
        account_id: "sca_123",
        amount: 100,
        note: "Loyalty reward",
        reference: "order",
        reference_id: "order_123",
      },
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { creditStoreCreditAccountWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  await creditStoreCreditAccountWorkflow(container)
    .run({
      input: {
        account_id: "sca_123",
        amount: 100,
        note: "Loyalty reward",
        reference: "order",
        reference_id: "order_123",
      },
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { creditStoreCreditAccountWorkflow } from "@medusajs/loyalty-plugin/workflows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  await creditStoreCreditAccountWorkflow(container)
    .run({
      input: {
        account_id: "sca_123",
        amount: 100,
        note: "Loyalty reward",
        reference: "order",
        reference_id: "order_123",
      },
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { creditStoreCreditAccountWorkflow } from "@medusajs/loyalty-plugin/workflows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    creditStoreCreditAccountWorkflow
      .runAsStep({
        input: {
          account_id: "sca_123",
          amount: 100,
          note: "Loyalty reward",
          reference: "order",
          reference_id: "order_123",
        },
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"creditStoreCreditAccountWorkflow","steps":[{"type":"step","name":"validateStoreCreditAccountInputStep","description":"This step validates the input for creating store credit accounts. It throws an\nerror if no input is provided or if any entry is missing a currency code.","link":"../core_flows.Store_Credit.Workflows_Store_Credit.validateStoreCreditAccountInputStep/page.mdx","depth":1},{"type":"step","name":"useQueryGraphStep","description":"This step fetches data across modules using the Query.\n\nLearn more in the [Query documentation](https://docs.medusajs.com/learn/fundamentals/module-links/query).","link":"../../../../Common/Steps_Common/functions/core_flows.Common.Steps_Common.useQueryGraphStep/page.mdx","depth":2},{"type":"step","name":"creditAccountStep","description":"This step credits one or more store credit accounts with transactions.\nThe step supports rollback by deleting the created transactions.","link":"../../../Steps_Store_Credit/functions/core_flows.Store_Credit.Steps_Store_Credit.creditAccountStep/page.mdx","depth":3}]}} />

## Input

<TypeList types={[{"name":"ModuleCreditStoreCreditAccount","type":"`ModuleCreditStoreCreditAccount`","optional":false,"defaultValue":"","description":"","expandable":false,"children":[]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="creditStoreCreditAccountWorkflow"/>
