import { TypeList, WorkflowDiagram } from "docs-ui"

# GetPromotionCodesToApplyStepOutput

 **GetPromotionCodesToApplyStepOutput**: `string`[]

The promotion codes to apply on the cart.

## Example

```ts
["PRO10", "SHIPFREE", "NEWYEAR20"]
```
