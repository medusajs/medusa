import { TypeList, WorkflowDiagram } from "docs-ui"

# CreateOrderWorkflowInput

 **CreateOrderWorkflowInput**: [CreateOrderDTO](../../../../order/interfaces/order.CreateOrderDTO/page.mdx) & [AdditionalData](../../../../types/HttpTypes/types/types.HttpTypes.AdditionalData/page.mdx)

The data to create an order, along with custom data that's passed to the workflow's hooks.
