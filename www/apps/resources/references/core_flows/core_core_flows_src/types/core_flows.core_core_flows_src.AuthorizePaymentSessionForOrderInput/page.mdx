import { TypeList, WorkflowDiagram } from "docs-ui"

# AuthorizePaymentSessionForOrderInput

 **AuthorizePaymentSessionForOrderInput**: `object`

The data to authorize a pending payment session for an existing order.
