import { TypeList, WorkflowDiagram } from "docs-ui"

# UpdateShippingOptionTypesWorkflowInput

 **UpdateShippingOptionTypesWorkflowInput**: `object` & [AdditionalData](../../../../types/HttpTypes/types/types.HttpTypes.AdditionalData/page.mdx)

The data to update one or more shipping option types, along with custom data that's passed to the workflow's hooks.
