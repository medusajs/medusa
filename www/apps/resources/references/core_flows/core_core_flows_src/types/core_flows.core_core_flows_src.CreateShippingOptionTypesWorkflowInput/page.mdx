import { TypeList, WorkflowDiagram } from "docs-ui"

# CreateShippingOptionTypesWorkflowInput

 **CreateShippingOptionTypesWorkflowInput**: `object` & [AdditionalData](../../../../types/HttpTypes/types/types.HttpTypes.AdditionalData/page.mdx)

The data to create one or more shipping option types, along with custom data that's passed to the workflow's hooks.
