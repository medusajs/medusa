import { TypeList, WorkflowDiagram } from "docs-ui"

# BeginOrderExchangeValidationStepInput

 **BeginOrderExchangeValidationStepInput**: `object`

The data to validate that an exchange can be requested for an order.
