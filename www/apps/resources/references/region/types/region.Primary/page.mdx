---
displayed_sidebar: regionReference
---

import { TypeList } from "docs-ui"

# Primary

 **Primary**: T extends `object` ? [ReadonlyPrimary](../region.ReadonlyPrimary/page.mdx)&#60;PK&#62; : T extends `object` ? [ReadonlyPrimary](../region.ReadonlyPrimary/page.mdx)&#60;PK&#62; \| `string` : T extends `object` ? [ReadonlyPrimary](../region.ReadonlyPrimary/page.mdx)&#60;PK&#62; : T extends `object` ? [ReadonlyPrimary](../region.ReadonlyPrimary/page.mdx)&#60;PK&#62; : `never`
