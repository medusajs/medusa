{"slug":"/references/inventory_next/types/inventory_next.CrossModuleJoinSpec","title":"CrossModuleJoinSpec","frontmatter":{"displayed_sidebar":"inventoryNextReference"},"toc":[{"title":"Example","id":"example","level":2}],"blocks":[{"kind":"markdown","html":"Metadata describing how to join and filter/sort against a table outside the module.\n\nThe join is translated into a correlated `EXISTS` subquery (for filtering)\nand/or a correlated scalar subquery (for sorting) against the root query,\nso the root rows are never multiplied and counts stay accurate.\n\nTarget table aliases match `target.table` directly (e.g. `product.handle`\nfor `target.table: \"product\"`).\n\nBehaviour callers should know about:\n- Filtering/sorting is executed as hand-built SQL against the target table.\n  Beyond the soft-delete guards below, no other MikroORM global filters\n  registered on the target entity are applied automatically (the target\n  lives in another module and is not a registered entity here). Any extra\n  scoping a module needs must be encoded into `target.filters`.\n- Soft-delete guards mirror the root query: soft-deleted links and targets\n  are excluded unless the query runs with `withDeleted`, in which case\n  they are included too.\n- Multiple independent targets are combined with AND by supplying several\n  top-level join specs (each without a `parent`).\n- Multi-hop filters chain joins through `parent`, correlating a join to a\n  parent target row rather than the root entity (e.g. customer -&#62; tier -&#62;\n  functionality). Chains can nest recursively to any depth. `parent` must\n  reference the parent join's `target.table`.\n- Each join must target a unique table within a single query.\n- Correlation uses a single primary key column; composite keys are not\n  supported."},{"kind":"heading","level":2,"text":"Example","id":"example"},{"kind":"markdown","html":"```ts\n{\n  link: {\n    table: \"customer_api_key\",\n    sourceKey: \"api_key_id\",\n    targetKey: \"customer_id\",\n  },\n  target: {\n    table: \"customer\",\n    filters: { email: \"user@example.com\" },\n  },\n}\n```"},{"kind":"typeList","types":[{"name":"CrossModuleJoinSpec","type":"`object`","description":"Metadata describing how to join and filter/sort against a table outside the module.\n\nThe join is translated into a correlated `EXISTS` subquery (for filtering)\nand/or a correlated scalar subquery (for sorting) against the root query,\nso the root rows are never multiplied and counts stay accurate.\n\nTarget table aliases match `target.table` directly (e.g. `product.handle`\nfor `target.table: \"product\"`).\n\nBehaviour callers should know about:\n- Filtering/sorting is executed as hand-built SQL against the target table.\n  Beyond the soft-delete guards below, no other MikroORM global filters\n  registered on the target entity are applied automatically (the target\n  lives in another module and is not a registered entity here). Any extra\n  scoping a module needs must be encoded into `target.filters`.\n- Soft-delete guards mirror the root query: soft-deleted links and targets\n  are excluded unless the query runs with `withDeleted`, in which case\n  they are included too.\n- Multiple independent targets are combined with AND by supplying several\n  top-level join specs (each without a `parent`).\n- Multi-hop filters chain joins through `parent`, correlating a join to a\n  parent target row rather than the root entity (e.g. customer -&#62; tier -&#62;\n  functionality). Chains can nest recursively to any depth. `parent` must\n  reference the parent join's `target.table`.\n- Each join must target a unique table within a single query.\n- Correlation uses a single primary key column; composite keys are not\n  supported.","optional":false,"defaultValue":"","example":"{ link: { table: \"customer_api_key\", sourceKey: \"api_key_id\", targetKey: \"customer_id\", }, target: { table: \"customer\", filters: { email: \"user@example.com\" }, }, }","expandable":false,"children":[{"name":"parent","type":"`string`","description":"When set, this join is correlated to the target row of the parent join\ninstead of the root entity. The value must match the parent join's\n`target.table`. Used for multi-hop filters such as filtering orders by a\nproduct's sales channel.","optional":true,"defaultValue":"","expandable":false,"children":[]},{"name":"link","type":"[CrossModuleJoinLink](/references/inventory_next/types/inventory_next.CrossModuleJoinLink)","description":"Describes a link table connecting the queried root entity to an external table.","optional":false,"defaultValue":"","expandable":false,"children":[{"name":"schema","type":"`string`","description":"PostgreSQL schema for the link table. Defaults to `public`.","optional":true,"defaultValue":"","expandable":false,"children":[]},{"name":"table","type":"`string`","description":"Link table name.","optional":false,"defaultValue":"","expandable":false,"children":[]},{"name":"sourceKey","type":"`string`","description":"Column on the link table referencing the correlation source — the queried\nroot entity when there is no `parent`, otherwise the parent join's target row.","optional":false,"defaultValue":"","expandable":false,"children":[]},{"name":"targetKey","type":"`string`","description":"Column on the link table referencing the external entity.","optional":false,"defaultValue":"","expandable":false,"children":[]}]},{"name":"target","type":"[CrossModuleJoinTarget](/references/inventory_next/types/inventory_next.CrossModuleJoinTarget)","description":"Describes an external table that can be filtered or sorted against.","optional":false,"defaultValue":"","expandable":false,"children":[{"name":"schema","type":"`string`","description":"PostgreSQL schema for the target table. Defaults to `public`.","optional":true,"defaultValue":"","expandable":false,"children":[]},{"name":"table","type":"`string`","description":"Target table name.","optional":false,"defaultValue":"","expandable":false,"children":[]},{"name":"primaryKey","type":"`string`","description":"Primary key column on the target table. Defaults to `id`.","optional":true,"defaultValue":"","expandable":false,"children":[]},{"name":"filters","type":"[FilterQuery](/references/inventory_next/types/inventory_next.FilterQuery)&#60;any&#62;","description":"Filters applied to the target table.","optional":true,"defaultValue":"","expandable":false,"children":[]}]}]}]}]}