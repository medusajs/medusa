import { TypeList } from "docs-ui"

# KnownModuleConfigs

 **KnownModuleConfigs**: [ModuleConfigForResolve](../types.ModuleConfigForResolve/page.mdx)&#60;keyof [ModuleOptions](../../CommonTypes/interfaces/types.CommonTypes.ModuleOptions/page.mdx) & string&#62;

Generates a union of typed module configs for all known modules in the ModuleOptions registry.
This distributes over all keys in ModuleOptions to create specific config types for each.
