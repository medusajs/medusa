import { TypeList } from "docs-ui"

# EntityConstructor

A type-only representation of a MikroORM entity. Since we generate
entities on the fly, we need a way to represent a type-safe
constructor and its instance properties.

## Type parameters

<TypeList types={[{"name":"Props","type":"`object`","description":"","optional":false,"defaultValue":"","expandable":false,"children":[]}]}  sectionTitle="EntityConstructor"/>

## constructor
