import { TypeList } from "docs-ui"

# DMLSchema

 **DMLSchema**: `Record<string, [PropertyType](../types.DmlTypes.PropertyType/page.mdx)&#60;any&#62; \| [RelationshipType](../types.DmlTypes.RelationshipType/page.mdx)&#60;any&#62;>`

Representation of DML schema. It must be a key-value pair
with string based keys and properties/relationships
as the value.
