type: object
description: The payment details.
required:
  - order_id
properties:
  order_id:
    type: string
    title: order_id
    description: The ID of the order associated with the payment collection.
  provider_id:
    type: string
    title: provider_id
    description: >-
      The ID of the payment provider to use for marking the payment collection
      as paid. If not provided, the `system` provider will be used.
x-schemaName: AdminMarkPaymentCollectionAsPaid
