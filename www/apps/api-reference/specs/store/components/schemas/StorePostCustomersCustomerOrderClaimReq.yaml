type: object
description: The details of the orders to claim.
required:
  - order_ids
properties:
  order_ids:
    description: The ID of the orders to claim
    type: array
    items:
      type: string
