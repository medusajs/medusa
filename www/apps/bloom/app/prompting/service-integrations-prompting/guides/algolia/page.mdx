export const metadata = {
  title: `Algolia Integration`,
}

# {metadata.title}

Add [Algolia](https://www.algolia.com/) to give your store powerful search.

Algolia gives instant search results with filters to help customers find products fast. Bloom can set it up for you in minutes.

{/* TODO: Add screenshot of Algolia search with instant results and filters */}

## Prerequisites

Before integrating Algolia, you need an [Algolia account](https://dashboard.algolia.com/users/sign_up).

Bloom will also ask for API credentials and index name when you add Algolia. You can get them from your Algolia dashboard.

### Algolia Credentials

To get your Algolia credentials:

1. Log in to your Algolia dashboard.
2. Choose the application you want to integrate with Bloom at the top left.
3. Go to Settings -> API Keys.
4. You'll need the Application ID, Search-Only API Key, and Admin API Key for the integration.

### Algolia Index

You also need to create an index in Algolia for products. To do this:

1. Click Search in the sidebar.
2. Click Index in the page's sidebar.
3. Use an existing index or create a new one for your products.

---

## Add Algolia to Your Store

Ask Bloom to add Algolia with details about how you want search to work. For example:

```bash
Add Algolia as a search provider for my store. I want customers to
be able to search products by name, description, and categories. Add
a search bar to the header of my store and show instant search results as customers
type.
```

Bloom will set up Algolia as the search provider for your store and configure the search experience based on your requirements.

---

## Enter Your Algolia Credentials

Bloom will ask for your Algolia credentials and index name. Enter the Application ID, Search-Only API Key, Admin API Key, and the index name you want to use.

Bloom stores these credentials securely and uses them to connect your store to Algolia.

<Note title="Technical Information">

Bloom stores your Algolia credentials as environment variables in your store. They are never exposed in your code or logs. See [Environment Variables](../../../../developers/environment-variables/page.mdx) for more details.

</Note>

---

## Test Algolia

After Bloom adds Algolia, test search in your preview store. Search for products using different keywords and check that results appear instantly as you type.

Make sure search finds products by name, description, and category like you asked.

You can ask Bloom to adjust the search experience. For example:

```bash
Add filters to the search results pop-up so customers can filter by category.
```

Bloom will update the Algolia integration to include filters in the search results as you requested.

---

## Publish Algolia Integration

Once you've tested the integration and you're ready to go live, you can publish your changes with your live credentials.

To publish the changes:

1. Click on the "Publish" button at the top right.
2. If you haven't published your store before, Bloom will ask you for some details about your store.
3. In the "Missing production values" section, enter the live credentials for Algolia. You can use the same credentials if you don't have separate ones for testing and production.
4. Click "Publish" to make your changes live.

Your Algolia integration will be published to your live store, and customers can start using it to search for products.

---

## Need Help?

If you face any issues while adding Algolia to your Bloom store, [contact support or submit feedback](../../../../help-and-feedback/page.mdx).
