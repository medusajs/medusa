# Medusa's packages use loose/optional peer dependencies; npm's strict peer
# resolution rejects some valid combinations that yarn accepts. This keeps
# `npm install` / `npm ci` working in the Docker build.
legacy-peer-deps=true
