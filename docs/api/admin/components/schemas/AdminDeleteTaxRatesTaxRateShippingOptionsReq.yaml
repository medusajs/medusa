type: object
required:
  - shipping_options
properties:
  shipping_options:
    type: array
    description: The IDs of the shipping options to remove association with this tax rate
    items:
      type: string
