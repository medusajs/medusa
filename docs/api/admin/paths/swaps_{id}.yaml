get:
  operationId: GetSwapsSwap
  summary: Retrieve a Swap
  description: Retrieves a Swap.
  x-authenticated: true
  parameters:
    - in: path
      name: id
      required: true
      description: The ID of the Swap.
      schema:
        type: string
  x-codeSamples:
    - lang: JavaScript
      label: JS Client
      source:
        $ref: ../code_samples/JavaScript/swaps_{id}/getundefined
    - lang: Shell
      label: cURL
      source:
        $ref: ../code_samples/Shell/swaps_{id}/getundefined
  tags:
    - Swap
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            properties:
              swap:
                $ref: ../components/schemas/swap.yaml
