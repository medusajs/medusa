post:
  operationId: PostReturnsReturnCancel
  summary: Cancel a Return
  description: Registers a Return as canceled.
  parameters:
    - in: path
      name: id
      required: true
      description: The ID of the Return.
      schema:
        type: string
  x-codeSamples:
    - lang: JavaScript
      label: JS Client
      source:
        $ref: ../code_samples/JavaScript/returns_{id}_cancel/postundefined
    - lang: Shell
      label: cURL
      source:
        $ref: ../code_samples/Shell/returns_{id}_cancel/postundefined
  tags:
    - Return
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            properties:
              order:
                $ref: ../components/schemas/order.yaml
