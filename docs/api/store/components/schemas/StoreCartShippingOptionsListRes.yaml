type: object
x-expanded-relations:
  field: shipping_options
  implicit:
    - profile
    - requirements
required:
  - shipping_options
properties:
  shipping_options:
    type: array
    items:
      $ref: ./PricedShippingOption.yaml
