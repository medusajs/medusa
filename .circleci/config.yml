version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      unit_test:
        executor: node
        steps:
          - checkout
          - run: lerna run test -- --passWithNoTests
