# AUTO-GENERATED by clai-helpers — DO NOT EDIT. Source: .claude/commands/speckit.diff.md
description = "Show what changed in a SpecKit feature between two pipeline stages. Wraps `git diff` over <stage>/<slug>/v<N> tags created by snapshot-stage.{sh,ps1}."

prompt = """
---
description: Show what changed in a SpecKit feature between two pipeline stages. Wraps `git diff` over <stage>/<slug>/v<N> tags created by snapshot-stage.{sh,ps1}.
---

## User Input

```text
$ARGUMENTS
```

**Argument forms** (positional):
- `<slug>` — diff between the two latest tags for this slug across any stage
- `<slug> <from-tag>` — diff from tag → HEAD
- `<slug> <from-tag> <to-tag>` — diff between two explicit tags

Examples:
- `/speckit.diff 001-orchestrator`
- `/speckit.diff 001-orchestrator spec/001-orchestrator/v1`
- `/speckit.diff 001-orchestrator plan/001-orchestrator/v1 plan/001-orchestrator/v2`

## Goal

ultrathink

> "Без diff'a нет дельты. Без дельты — нет понимания, что ты переделал." — Valera, on iteration discipline.

Show the user what changed between two pipeline-stage snapshots of a feature. Built on git tags written by `snapshot-stage.{sh,ps1}` per constitution Principle VII (Artifact Versioning).

## Operating Constraints

**STRICTLY READ-ONLY**. No file writes, no commits, no tag creation. Just display.

## Execution

### 1. Parse Arguments

Split `$ARGUMENTS` by whitespace. First token = slug. Optional 2nd/3rd = from-tag/to-tag.

Abort with usage message if slug missing.

### 2. Resolve Tags

If user passed explicit tags, use them. Else discover via git:

```bash
git tag --list "*/<slug>/v*" | sort -V
```

The naming convention is `<stage>/<slug>/v<N>` where stage ∈ {spec, clarify, plan, tasks, review}.

- **Default behaviour (no explicit tags)**: pick the two latest tags by tag creation time. If only one tag exists, diff from that tag → HEAD. If zero tags, abort with: "No snapshot tags found for `<slug>`. Did the speckit pipeline ever run for this feature?"
- **One tag provided**: diff from that tag → HEAD.
- **Two tags provided**: diff between them.

### 3. Compute Diff

Run:

```bash
git diff --stat <from> <to> -- "specs/<slug>/"
git diff <from> <to> -- "specs/<slug>/"
```

Scope is restricted to `specs/<slug>/` — the diff doesn't include changes elsewhere in the tree even if other files moved between snapshots.

### 4. Render Report

Output structured markdown:

```markdown
# Diff: <slug>

**From**: `<from-tag>` (<short-sha>, <iso-date>)
**To**: `<to-tag-or-HEAD>` (<short-sha>, <iso-date>)
**Scope**: `specs/<slug>/`

## Files Changed

<output of git diff --stat, formatted as table>

| File | +/- | Lines |
|------|-----|-------|
| spec.md | ++ | +12 / -3 |
| plan.md | + | +5 / -0 |
| tasks.md | +- | +8 / -2 |

## Section-Level Summary

For each changed `*.md` file, group hunks by markdown header (## section). Report:
- Sections added
- Sections removed
- Sections modified (with line count)

## Full Diff

<paste full `git diff` output>
```

### 5. Suggest Next Actions

Based on what changed, suggest which gate to re-run:

| What changed | Suggested next |
|--------------|---------------|
| `spec.md` | Re-run `/speckit.clarify` if needed → `/speckit.plan` to refresh dependent docs |
| `plan.md` | Re-run `/speckit.tasks` (plan changes invalidate task graph) |
| `tasks.md` | Re-run `/speckit.analyze` + `/speckit.review` (gate must re-pass) |
| `reviews/*.md` | None — reviews are evidence, not artifacts |

## Operating Principles

### Stable IDs
Tag names are deterministic — `spec/001-orchestrator/v3` means exactly that. Don't paraphrase or shorten in output.

### Useful, not exhaustive
If diff > 1000 lines, render `--stat` + section-level summary only. Offer to print full diff on user request.

"""