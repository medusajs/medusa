# AUTO-GENERATED by clai-helpers — DO NOT EDIT. Source: .claude/commands/orch.run.md
description = "Start orchestration run via MCP — dispatches AI tools through the speckit pipeline"

prompt = """
---
description: Start orchestration run via MCP — dispatches AI tools through the speckit pipeline
---

# Orchestrator Run

Use the `orch_run` MCP tool to start a multi-model orchestration run.

## Input

$ARGUMENTS

## Instructions

1. Parse the user's input to determine:
   - **description**: What feature/task to build
   - **specDir**: If user mentions an existing spec directory (e.g., `specs/001-auth/`), pass it
   - **dryRun**: If user says "dry run", "plan only", or "preview" — set to true

2. Call the `orch_run` MCP tool with:
   ```json
   {
     "description": "<parsed description>",
     "projectDir": "<current working directory>",
     "dryRun": false
   }
   ```

3. Report the result to the user:
   - On success: show runId, status, pipeline plan
   - On failure: show error, suggest fixes (tool not found? config missing?)

## Examples

```
/orch.run Add JWT authentication with refresh tokens
/orch.run Add dark mode --dry-run
/orch.run Implement WebSocket real-time sync --spec-dir specs/003-websocket/
```

## Speckit Integration

If the user has existing speckit artifacts:
- `--spec-dir specs/XXX/` → orchestrator auto-detects existing spec.md/plan.md/tasks.md and skips completed stages
- `--from <stage>` → explicitly start from a specific pipeline stage

## Notes

- The orchestrator MCP server must be running (`orch mcp-serve` or configured in Claude Code MCP settings)
- At least one AI CLI tool must be enabled in `orch.config.yaml`
- Use `/orch.status` to check progress after starting a run

"""