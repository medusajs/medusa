# AUTO-GENERATED by clai-helpers — DO NOT EDIT. Source: .claude/commands/orch.tools.md
description = "List available AI tools in the orchestrator via MCP — shows health, strengths, providers"

prompt = """
---
description: List available AI tools in the orchestrator via MCP — shows health, strengths, providers
---

# Orchestrator Tools

Use the `orch_tools_list` MCP tool to see registered AI coding tools and their status.

## Input

$ARGUMENTS

## Instructions

1. Parse the input:
   - If user says "health", "test", "check" → set `healthCheck: true`
   - Otherwise → `healthCheck: false` (faster, no spawning)

2. Call the `orch_tools_list` MCP tool:
   ```json
   {
     "healthCheck": false
   }
   ```

3. Format as a table:
   ```
   Registered Tools:
     ✓ claude    (anthropic)  priority: 1  strengths: backend, review, spec, security
     ✓ gemini    (google)     priority: 2  strengths: frontend, review, spec
     ✓ copilot   (github)     priority: 3  strengths: backend, frontend, review
     ✗ opencode  (local)      priority: 4  strengths: backend, database, devops  [disabled]
     ✗ qwen      (alibaba)    priority: 5  strengths: backend, database  [disabled]
   ```

4. If `healthCheck: true`, also show response times:
   ```
     ✓ claude    healthy (1234ms)
     ✓ gemini    healthy (890ms)
     ✗ copilot   unhealthy: "command not found"
   ```

## Examples

```
/orch.tools
/orch.tools health
/orch.tools check all
```

"""