# AUTO-GENERATED by clai-helpers — DO NOT EDIT. Source: .claude/commands/orch.status.md
description = "Check orchestration run status via MCP — shows pipeline progress, task completion, active lanes"

prompt = """
---
description: Check orchestration run status via MCP — shows pipeline progress, task completion, active lanes
---

# Orchestrator Status

Use the `orch_status` MCP tool to check the current state of an orchestration run.

## Input

$ARGUMENTS

## Instructions

1. Parse the input:
   - If user provides a run ID → pass it
   - If empty → omit runId to get the latest run

2. Call the `orch_status` MCP tool:
   ```json
   {
     "runId": "<optional run ID>"
   }
   ```

3. Format the result as a status board:
   ```
   Run: <runId> | Status: <status> | Elapsed: <time>

   Pipeline:  [=====>    ] 5/7 stages
   Ensemble:  [===       ] 3/10 tasks (2 lanes active)

   Active Lanes:
     Lane 1 [DB]  → claude  T014 running (45s)
     Lane 2 [BE]  → gemini  T019 running (30s)
   ```

## Examples

```
/orch.status
/orch.status abc12345
```

"""